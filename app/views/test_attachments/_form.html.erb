<%= form_for(@test_attachment) do |f| %>
  <% if @test_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_attachment.errors.count, "error") %> prohibited this test_attachment from being saved:</h2>

      <ul>
      <% @test_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attachment %><br>
    <%= f.text_field :attachment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
