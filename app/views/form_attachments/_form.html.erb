<%= form_for(@form_attachment) do |f| %>
  <% if @form_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form_attachment.errors.count, "error") %> prohibited this form_attachment from being saved:</h2>

      <ul>
      <% @form_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :form_id %><br>
    <%= f.number_field :form_id %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
